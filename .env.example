# API URL for the OpenSocial backend
#
# Development: Leave this unset or empty to use relative URLs with Vite's dev proxy
# Production: Set to your backend API URL (e.g., https://api.example.com)
#
# Example for production deployment on GitHub Pages:
# VITE_API_URL=https://your-backend-api.example.com
#
# Example for local development (can be left unset):
# VITE_API_URL=
VITE_API_URL=

# Base path for deployment (optional)
#
# Development: Leave this unset (defaults to '/')
# Production on GitHub Pages (subpath): Set to '/<repo-name>/'
# Production on custom domain (root): Leave unset or set to '/'
#
# Example for GitHub Pages at username.github.io/open-social-web:
# VITE_BASE_PATH=/open-social-web/
#
# Example for root domain deployment:
# VITE_BASE_PATH=/
